export interface EnvironmentVariablesInterface {
    db_host: string,
    db_port: number,
    db_database: string,
    db_username: string,
    db_password: string,
    port: number;
    token_secret: string;
    gmail_user: string;
    gmail_password: string;
}
